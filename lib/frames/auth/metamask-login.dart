import 'package:flutter/material.dart';

class LoginMetamask extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Login with LoginMetamask'),
      ),
      body: const Center(
        child: Text('Login with LoginMetamask'),
      ),
    );
  }
}